/*
To find the max length o substring that repeats 'k' times in the given string
*/
#include <stdio.h>
#include<stdlib.h>

struct hash{
	int index;
	int val;
	struct hash *next;
};

int power(int n)
{
	out = 1;
	for(int i=0; i<n; i++)
		out *= 2;
	return out;
}

int check(char str[], int n, int l, int k)
{
	
}

int bs(char str[], int n, int k, int i, int j)
{
	if(i==j)
		return i
	
}
